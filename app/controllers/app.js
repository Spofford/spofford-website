import Ember from 'ember';

const { Controller } = Ember;



export default Controller.extend({
  aboutContent: function() {
    
    //client.getEntry('header1')
    //.then((entry) => console.log(entry))
    //.catch(console.error)
  }.on('init')

});
